# Install AsyncStorage - Quick Guide

## 📦 Installation Command

Run this command in your terminal:

```bash
npm install @react-native-async-storage/async-storage
```

Or if using Expo:

```bash
npx expo install @react-native-async-storage/async-storage
```

## 🔄 After Installation

1. **Restart your development server:**
   ```bash
   # Stop current server (Ctrl+C)
   npm start
   ```

2. **Clear cache if needed:**
   ```bash
   npm start -- --clear
   ```

## ✅ Verification

After installation and restart, the onboarding flow will work:
- First time users see Welcome screen
- Returning users go directly to Events screen

## 🎯 What This Enables

- ✅ Local data storage on device
- ✅ User profile persistence
- ✅ Onboarding flow
- ✅ Offline data access

That's it! Just one command and restart. 🚀
